# JFSN — Improvement List
**Updated:** 2026-07-27 (Decade Display Redesign)

A living list. Add to it. Cross things off. This is the backlog.

---

## 🔴 Do soon

- [ ] **Ingest 40 new artworks (art1085–art1124)** — **2026-07-19** — Automated intake pipeline validated (Phase 2.3: `python3 artworks/ingest.py`, Phase 2.4: `python3 tools/intake/intake_finish.py`). 20 pieces at 3'×4', 20 at 2'×3', all double-sided. Test setup complete (3 placeholders on jfsn.com verify the flow). Suggested workflow: batch 10 at a time, photograph + drop to inbox, auto-ingest, manually edit 10 JSON sidecars (title/year/materials/themes), finish & deploy. ~4 deploy cycles expected. [See intake_pipeline session memory for full details.](https://claude.ai/session/...) Delete test entries (art9901–art9903) before starting real ingestion.

---

## 🟡 High value, no deadline

### Content
- [ ] **Oral history — unanswered questions** — see `docs/sources/oral-history/master-notes.md` § "Unresolved Questions" for priority list. Top item: why did he keep going after the Rauschenberg realization?

### Technical
- [ ] **Artwork permalink canonical URL — open architectural question, deliberately parked 2026-07-21.** Audited `artwork.html?id=artNNNN` (canonical, linked everywhere) vs. the 1,086 generated `artworks/pages/artNNNN.html` static pages (more durable/no-JS-complete, but unlinked from the live site). Investigated migrating canonicality to the generated pages (feature-parity table, front/back parity, migration risk, redirect feasibility) but explicitly did not implement it — `current.html`'s river experience is tightly coupled to the `artwork.html?id=` URL form, and the benefit (crawlers/no-JS visitors get a complete record without JS) hadn't been shown to outweigh the migration cost. `artwork.html?id=` remains canonical. Do not assume this was resolved either way — revisit only if there's a concrete reason (SEO problem observed, no-JS complaint, etc.), not just because the generated pages exist. See session memory `session_2026_07_21_artwork_permalink_audit_and_fix.md`.
- [x] **`index.html`'s inline `<style>` (29KB current, 34KB at audit 2026-06-22) — documented as acceptable pattern.** Investigated in depth 2026-06-22, concluded NOT safely reducible by simple dedup. Programmatically diffed every selector appearing in more than one block (9 candidates: `.hero-cta-ghost`, `.reveal-section` + its `.is-visible`/`.reveal-delay-1/2/3` variants, `.wtb-card`, `#hero-scroll-cue`). Every single one turned out to be **intentional layering, not redundancy** — e.g. `.hero-cta-ghost`'s visual properties (color/border/font) live in one block while its motion timing and `@media (prefers-reduced-motion: reduce)` override live in another, added in different sessions targeting different concerns of the same component. Merging blocks would be a zero-risk no-op (CSS cascade only cares about source order, not tag count) but **would not reduce byte count** — the 35KB audit threshold is on bytes, not tag count. Genuinely cutting bytes would mean rewriting cascading rules, which risks regressing the most-edited page on the site (245+ commits) for minimal savings. **Decision (2026-07-28):** Accept this as an intentional pattern. Documented in `CLAUDE.md` that pages carrying full animation systems (hero arrival, room choreography, scroll-parallax, reduced-motion overrides) legitimately accumulate layered CSS, and the 35KB threshold is calibrated to catch bloat, not regulate intentional complexity. Only index.html currently justifies this; if another page approaches the limit, that signals investigation needed.
- [x] **17 Session-65 interaction scripts audited in full, 2026-06-22** (resolves the earlier same-day flag, which was based on a stale Don'ts list — corrected separately in `DESIGN-SYSTEM.md`; generic motion like parallax/scroll-reveal/scale-transform is NOT banned per `CLAUDE.md`'s actual current stance, so that was never the real question). Read every file and checked live attribute/class usage against each one. Findings:
  - **Two real, fixed bugs:** (1) `lightbox.js` unconditionally hijacked every `.thumb__link` click (21 pages) with `preventDefault()`, racing `page-transitions.js`'s own click handler — every thumbnail click briefly flashed a bare image-only modal (no title/year/medium) before the real navigation won a moment later. Confirmed live via dispatched click events. Fixed: `lightbox.js` no longer touches `.thumb__link`; the modal now only attaches to the (currently unused) `[data-zoomable]` opt-in. (2) `scroll-to-top.js` injected its own floating button at almost the exact position/size as `_shared/footer.html`'s `#btt-float` (28px/44px vs. 24px/48px from bottom-right) — two overlapping buttons on every page past 300px scroll. Disabled `scroll-to-top.js` (emptied, same pattern as the already-disabled `keyboard-shortcuts.js`); `#btt-float` is the one actually wired into the canonical footer.
  - **Confirmed 100% dead code (zero matching attributes/classes anywhere in the live HTML), harmless but parsed/executed on every page for nothing:** `advanced-interactions.js` (drag-drop/long-press/context-menu), `infinite-scroll.js`, `parallax.js`, `scroll-reveal.js`, `swipe-gestures.js`, `form-validation.js`, `search-highlight.js`, `search-breadcrumb.js` (reachable only via a specific sessionStorage sequence nothing currently sets). `micro-interactions.js` (1,326 lines, ~40 functions, all invoked unconditionally on every page) was the same story at larger scale — re-implements many of the same features under different selectors, all opt-in via classes/IDs that don't exist in the live HTML (`.hero-parallax-image`, `.grid-stagger`, etc.), so none of it did anything, including its own `setupScrollToTop()`. **DELETED in commit `5fcedd17` (50KB saved).**
  - **Active, harmless, no artwork-honesty issue:** `hover-preview.js` (supplementary metadata tooltip, doesn't hide anything), `toast.js` (passive utility called by other scripts), `analytics.js` (live on production, sends batched events via `sendBeacon` to `/analytics` — which doesn't exist as a backend on this static site, so it silently no-ops; duplicates GoatCounter's actual job for no benefit, but causes no visible problem), `image-prefetch.js` (prefetches adjacent artwork images; one fallback path guesses `.jpg` instead of the real `.avif` extension — harmless since it's just a failed prefetch, not user-visible).
  - **None of the 17+1 files filter, recolor, distort, or tilt an artwork image, or hide a work's title/year/medium behind a hover-only state** — the actual non-negotiable rule. Confirmed clean.
  - **2026-06-23 — decided and done.** Jeff: delete the ~8 confirmed-dead files. Independently re-verified zero live references for every opt-in selector (`data-drop-zone`, `data-long-press`, `data-infinite-scroll`, `data-parallax`, `data-reveal=`, `.reveal-fade`/`.reveal-up`/`.reveal-scale`, `data-swipe-*`, `data-validate`, `data-search-result`, `data-work-title`, `breadcrumb-container`) across all 39 root HTML pages + all 1,084 generated artwork pages before deleting. Removed `advanced-interactions.{js,css}`, `infinite-scroll.{js,css}`, `parallax.{js,css}`, `scroll-reveal.{js,css}`, `swipe-gestures.{js,css}`, `form-validation.{js,css}`, `search-highlight.{js,css}`, `search-breadcrumb.{js,css}` (16 files) and their `<script>`/`<link>` tags from all 39 pages. `tools/generators/gen-artwork-pages.py`'s template never referenced them, so the 1,084 generated pages needed no changes. `micro-interactions.js` (the larger, same-pattern dead file) was left in place — not part of this specific decision, still open.
- [ ] **Grid/search/favorites year labels** (optional follow-up to provenance) — these still show the bare decade year ("1990"); only artwork detail pages + API carry the honest "1990s (est.)". Could extend `year_display` to grids, but it adds visual noise to terse captions — deferred pending Jeff's call.
---

## 🟢 Nice to have, low urgency

Nothing currently queued.

---

## ✅ Completed

History lives in `git log` — `git log --oneline --all` for the full record. A few recent highlights for orientation:

- **2026-07-27 (decade display redesign)** — **Changed footer decade presentation from six labels to ONE large active decade.** Replaced horizontal row of all six decade labels (1970s–2020s) with single, monumental decade numeral centered in artwork.html sidebar footer. Decade scales responsively (`clamp(80px, 18vw, 180px)`) from mobile to desktop. Color `#241e19` (very dark brown) reads subtle against black background but remains readable. "YEARS ARE DECADE ESTIMATES" subtitle kept small and secondary below. Smooth transitions (0.4s ease) when navigating between artworks from different decades. Respects `prefers-reduced-motion`. Systemic change in shared artwork.html template — applies across all 1,086 artwork pages without duplication. CSS redesigned `#decade-axis` as flex container; JavaScript simplified to update single `#decade-numeral` element instead of generating positioned spans for all decades. Verified responsive across desktop/tablet/mobile and across multiple decades (art0010: 1970s, art0200: 1990s, art0379: 2020s). Commit `7a9adbda`, deployed and live, all smoke tests passed.

- **2026-07-27 (artwork positioning & butterfly animation)** — **Two interactive enhancements shipped.** (1) **Artwork repositioning** (`33184c4b`): Changed `main` grid's `align-items` from `center` to `start` on artwork.html, eliminating the large dead zone above the artwork that was created by vertical centering. Artwork now claims the top-left of the viewport immediately after header padding. Verified across portrait/landscape/square orientations and desktop/tablet/mobile viewports — aspect ratios preserved, PREVIOUS/NEXT controls unaffected. (2) **Butterfly navigation animation** (`c25eebee`): Added erratic flight animation to site menu entrance. When drawer opens, butterfly emerges from right edge, flies through sidebar with unpredictable motion for 1.6 seconds, then settles faintly mid-drawer at ~18% opacity. Size: 64×72px desktop / 48×54px mobile. Image loaded from `/assets/images/jfsn-butterfly-2.png`. Respects `prefers-reduced-motion` media query; never obstructs CLOSE button. Both features deployed and live at jfsn.com, all smoke tests passed.

- **2026-07-27 (archive filter audit & correctness fix)** — **Complete audit + minimal repair of archive.html filter system.** Two bugs found and fixed: (1) DECADE filter returned zero results due to type mismatch in buildChips() — numeric decade values (1970, 2000, etc.) were being treated as objects with missing `.name` properties, causing `state.add(undefined)`. Fixed: added `|| typeof v === 'number'` to value-extraction logic. (2) AUDIO filter missing from 8 shared management paths despite being in filtered() + HTML markup — absent from collapse-groups, visibility checks, chip count, empty-result message, filter labels, clearAll(), and save/restore. Fixed: systematic audit of all filter-state manipulation paths, targeted 8 additions across archive.html. Verification: 44 individual filter values tested for exact match (100% pass), 30 multi-value combination tests (100% pass), 10 state/UX operations (100% pass), zero regressions on responsive/JS/smoke tests. Commit `359a9a02`, deployed and live, all smoke tests passed.

- **2026-07-27 (text-link underline unification)** — **Generalized the animated text-link underline into a single shared rule, applied site-wide.** Investigated about.html's inline/editorial links (e.g. "Read the full oral history →") as the canonical JFSN link interaction — a 1px accent bar growing in from the left on hover/focus — and found only 4 of 14 pages (about, archive, artwork, flooded-wing) actually used it; the other 8 fell back to a plain browser underline (a leftover from the 2026-07-23 partial color-only fix, see below). Extracted the rule into `_shared/text-links.css`, linked from all 14 production pages, removing each page's duplicated local copy — fixing two real inconsistencies along the way (artwork.html had `prefers-reduced-motion` gated backwards, silently breaking the hover underline for reduced-motion users; several pages were missing the `:focus-visible` box-shadow). Added targeted `::after{content:none}` exclusions for image/card links (`.card a`, `.op a`, `.work a`, `#related-audio-list a`, the Chromatic River preview, index.html's CTA button) so the treatment stays scoped to prose, not thumbnails or buttons. Caught and fixed a real pseudo-element conflict in the process: index.html's 5 room-door tiles reuse `::after` for their own ghosted-preview-image effect, which the shared rule's `width:0/height:1px` would have silently collapsed into a 1px sliver — fixed with an explicit override, verified via computed style. `current.html` untouched (all its links are HUD/room-nav/artwork-card). Commit `273356b6`, deployed live, all smoke tests passed.

- **2026-07-25 (audio discovery Phase 3)** — **Three discovery features for audio-enabled artworks.** (1) **Archive filter** (`8a0c936c`): New "HAS AUDIO" filter in archive.html showing 2 audio works; integrates with existing chip UI and filtering system. (2) **Timeline highlights** (`a3e1fd68`): Orange accent markers above audio works in The Current's river visualization (main canvas + minimap); visual distinction as works flow through timeline. (3) **Related audio works** (`433efae9`): "Other Works with Audio" section on artwork pages showing up to 6 related pieces in grid layout with thumbnails/title/year/material; hidden if viewing non-audio work. All features auto-scale as more audio recordings are added to config. Deployed live jfsn.com, smoke tests passed.

- **2026-07-25 (audio player Phase 1 + Phase 2)** — **Built native HTML5 audio player component + wired to config metadata.** (1) **Phase 1** (`479cb5e7`): Minimal, accessible player component (play/pause, timeline scrubbing, volume, keyboard shortcuts: Space/Arrow keys). Built `_shared/audio-player.js` (~170 lines) and `_shared/audio-player.css` (~140 lines). Added to artwork.html with hardcoded `/audio/who-i-am.m4a`. Keyboard accessible, touch-friendly, respects `prefers-reduced-motion`. (2) **Phase 2** (`f40a5398`): Made player data-driven. Added audio metadata fields to config/current.json (`audio`: filename, `audio_title`: display name). Updated artwork.html JS to read config and show/hide player dynamically — appears only on artworks with audio metadata (currently art0379 = who-i-am.m4a). Fixed CSS bug where HTML `hidden` attribute wasn't hiding the player (`display: flex` override). CACHE_V bumped for CSS rebuild. (3) **Live verification:** art0379 shows player with correct 33.8s duration; art1086 (no audio) hides player. Commits deployed to HostGator, all smoke tests passed.

- **2026-07-24 (wing hero polish)** — **Four fixes to homepage wing images: overflow, positioning, brightness, glow effects.** (1) **Overflow fix** (`bd76b161`): flexbox `min-width:0` on items + `max-width:100%` on images prevented wide-aspect works from clipping off-screen. Root: flexbox default `min-width: auto` on replaced elements. (2) **Positioning spread** (`aac7b096`): `#hang justify-content:center` → `space-between` pushes wings to viewport edges; center work anchors true middle. (3) **Brightness boost** (`d5f822bd`): removed `filter:brightness(0.3)` from wings, now 100% luminance. (4) **Dynamic glow effects** (`f0ca67b5`): 5 colorful drop-shadow signatures per artwork ID (deterministic hash), e.g., art0010 always gets magenta+cyan, art0030 always gets purple+lime. Wings now visually "signed" as they rotate. All four live, verified, CACHE_V bumped. The earlier wing-crossfade overflow bug is now fixed (was flagged 2026-07-24 as awaiting direction; resolved same session).

- **2026-07-24 (TV hero broadcast — variety, pacing, size)** — **Five directed iterations on the homepage TV static/broadcast prop, each proposed, confirmed, and deployed separately.** (1) Initial inspection + proposal (2x size, 40-60 works, lazy-load, faster timing) approved and shipped: TV width doubled (`clamp(240px,28vw,380px)`), artwork pool expanded 9→54 works (sampled every 20 across the archive), lazy-loading (5 preload, rest on-demand per broadcast), faster cadence (commit `adcc6192`). (2) Follow-on bug found and fixed during visual verification: central hero image (`art1010`) rendered smaller than its flanking wings — root cause was a reference to a nonexistent `art1010-hero-lcp.avif`/`-m.avif` pair (never existed in the repo), first patched to `medium/art1010-hero.avif` (commit `ab3e0809`), which then surfaced a **second, deeper bug**: a stale service-worker cache entry serving pre-06-06 bytes at that same URL (fixed via `CACHE_V` bump, commit `9cfdffa2`), which in turn revealed the *real* root cause — `medium/art1010-mobile.avif` is unrelated portrait-oriented content (583×1882), not a matching crop, so the responsive `srcset` occasionally picked a badly mismatched image. Final fix: all three hero images (center + both wings) standardized on `medium/art1010.avif` (900×792, matches the hardcoded width/height attributes exactly), srcset dropped (commit `ab5365c9`). (3) Jeff requested 3x more variety + less static + more randomness: pool expanded 54→153 works (sample every 7), static-only gap shortened 1-3s→0.2-1.2s, timing ranges widened for organic variance (commit `bb54e30f`). (4) Jeff requested artwork last longer + even less static + more diverse static texture: hold time increased 0.25-1.6s→2.5-5.5s, gap shortened further to 0.1-0.5s, noise-overlay floor lowered (0.5→0.28) so artwork reads clearer at peak signal, and static grain now drifts organically (brightness/density random-walk) plus a rare ~1% brief horizontal interference band for texture variety (commit `e52997be`). (5) Jeff requested TV 50% smaller: `clamp(240px,28vw,380px)`→`clamp(120px,14vw,190px)` — exactly reverses step (1)'s doubling back to the original size (commit `49a0f282`). All five iterations verified live on jfsn.com via Browser-pane screenshots + `getBoundingClientRect()`/`naturalWidth` checks before considering each done, not just local preview. **Separate bug found but not fixed** (flagged to Jeff, awaiting direction): the pre-existing wing-crossfade feature (rotates random archive works into the flanking hero images over time) has no `max-width` constraint on `.wing img` — a sufficiently wide/panoramic work can render wider than the viewport and clip off-screen entirely. Confirmed live: a wide work's right edge landed 290px past a 1500px viewport edge.

- **2026-07-23 (search-index cleanup)** — **Fixed obsolete search-indexed URLs (5 new 301 redirects + 1 broken timeline redirect).** Audited and fixed SEO-critical redirect chains preventing proper Google indexation. (1) **Five 301 redirects added** (commits `ec69b87b`): old portfolio/career pages (`art.html`, `portfolio.html`, `design.html`, `contact.html`, `ai.html`) now redirect to functionally equivalent or historically contextual destinations (`archive.html`, `working-history.html`, `about.html`). All live-tested: 301 → 200, zero chains. (2) **Timeline-fixed.html broken redirect fixed** (commit `d31b2599`): was `301 → timeline.html → 404`. `timeline.html` deliberately retired 2026-06-24 (no replacement planned per Jeff's decision). Changed to `HTTP 410 Gone` — signals intentional discontinuation to search engines, preserves link equity, enables future recreation if needed. Live-tested: 410 returned correctly. **Impact:** Fixes redirect chains blocking Google's proper indexation of these obsolete URLs; ensures 1,086 current-site URLs remain canonical and discoverable. Deployment: both commits deployed 2026-07-23, all smoke tests passed, verified live.

- **2026-07-23 (hero family polish)** — **Two-round creative pass on the room-hero family (about / the-studio / hall-of-openings / guernica-passage).** (1) **Vignette contrast fix** (`4213395b`): the shared radial-ellipse scrim was tuned against dark images and barely darkened guernica's mostly-white Guernica reproduction or the-studio's saturated photos — added a flat low-opacity wash beneath the ellipse on both so text holds a contrast floor. (2) **Per-room hero motion** (`98be021b`): scroll-parallax baseline (~40% rate, rAF-throttled, `--hero-parallax-y`) + one signature effect per room, all `prefers-reduced-motion`-gated — guernica: one-time arrival shudder; hall-of-openings: focus-pull (extra blur at rest easing to the composite's native softness on scroll); the-studio: ported the homepage ambient cursor-glow; about: two-beat headline settle-then-stamp. Gotcha: replaced `<img>` heroes need explicit width/height (offsets don't stretch them). **Flooded Wing deliberately left as-is** (Jeff's call — strongest hero; water-motion idea rejected as too close to using real loss for effect). CACHE_V bumped. Also added OG tags + print styles sitewide (`8c720ca8`) earlier the same day. Standing top opportunity: the four `<!-- AUDIO -->` room placeholders await Jeff's recorded voice.
- **2026-07-23 (discoverability + image optimization)** — **The Current's river bars now read as artworks; Hall of Openings cleaned up; all images optimized.** (1) **The Current discoverability:** reframed hint text to lead with reward ("MOVE ACROSS THE RIVER TO REVEAL THE WORKS"), implemented first-arrival demo (one bar rises/brightens, card appears ~900ms after intro closes, settles permanently, cancels on any interaction), year numeral now tracks focused work's decade. (2) **The Current prominence:** preview card enlarged ~45–50% (232→340px desktop, 196→280px mobile) with soft dark separation halo. (3) **Hall of Openings cleanup:** removed large Roman numeral dividers (rendered as vertical lines), added horizontal text padding (16px) to section descriptions. (4) **Image format migration:** all 7 main PNG files converted to AVIF (current 250K→26K, flooded 916K→83K, guernica 82K→22K, hall 251K→35K, studio 1.1M→92K, vault 2.5M→381K, me-for-website 406K→43K) — total 5.9M→0.65M (89% reduction). PNG originals deleted from repo. (5) **Link hover styling:** hover underline color changed from white to orange (`text-decoration-color: var(--accent)`) on the-studio.html, guernica-passage.html, current.html for readability. Commits `d7a65466`–`9453e356`, all deployed live with passing smoke tests.

- **2026-07-22 (homepage Archive CTA)** — **Jeff's direction: the Archive (1,086 works, search, catalog) is the functional core of the site but was the least visually prominent link on the homepage** — buried at the very bottom in a small-text "research path" row, deliberately styled (per the code's own comment) as the most neutral of three near-identical links. Discussed three options (restyle in place / promote to door-card / move up near hero); Jeff chose "move up," then refined placement twice more in direct iteration. Final state: a bordered, orange-accented "Enter the Archive · 1,086 works · catalog, search, data" CTA sits just below the fold, right after the hero's bouncing scroll-cue arrow and before the "why" paragraph — the first thing visible once a visitor starts scrolling (commits `d3e7a485` → `a24e1461`). The original small-text Archive link in the bottom research-path row was then removed as a now-redundant duplicate, leaving Working History and About there with their letter-spacing "depth" styling reflowed for two links instead of three (commit `f9b97d3f`). All three iterations verified via direct DOM/rect inspection (`compareDocumentPosition`, `getBoundingClientRect`) rather than trusting screenshots — this session's Browser-pane tool had confirmed rendering reliability issues (see the back-button entry above), so structural verification was used instead where it mattered.

- **2026-07-22 (homepage back-button + hover-glow fixes)** — **Two real homepage bugs found and fixed after an extended debugging session; a third fix attempt (SW cache bump, commit `f68d8ada`) turned out to be unrelated to the actual reported issue.** (1) **Ambient-light hover glow clipped on wide desktop screens** (commit `d077fc6d`): `#ambient-light`'s mouse-follow glow was `position:absolute;inset:0` inside `.threshold`, which has `max-width:1180px` and is centered — on desktop viewports wider than that, the glow hit a hard edge well before reaching the actual screen edges, reading as "cut off on the sides." Fixed by making the glow span `100vw` (full viewport width) while keeping its vertical bounds tied to `.threshold`; split the mousemove handler's coordinate tracking so the glow uses viewport-relative coordinates while the separate `#work` parallax-tilt effect keeps its original `.threshold`-relative ones. Confirmed fixed live. (2) **Room doors (the five room links below the hero) went invisible after using the browser back button** — took four fix attempts to find the real cause because every earlier theory was plausible but wrong: first a `prefers-reduced-motion` CSS specificity issue (real, fixed, commit `8c9bf277`, but not the reported bug), then two attempts assuming an IntersectionObserver/scroll-restoration timing race (commits `e8b6dffc`, `44c024a1`, `f0f73fdf` — each an improvement but none the actual fix). The real cause, found only via `document.elementFromPoint()` direct hit-testing at the exact blank pixels: the clicked door link retained its `threshold-active` class from the click-departure animation (`threshold-passage` keyframe, `animation-fill-mode:forwards`, ending frozen at `opacity:0` by design — normally invisible since the page navigates away within 400ms of the class being added). This state survived the user's back-navigation even though `performance.getEntriesByType('navigation')[0].type` reported it as an ordinary `'reload'`, not `'back_forward'` — meaning the existing `e.persisted`-gated pageshow cleanup used elsewhere on the page never ran for this case. Every earlier check in the session had only inspected the *parent* `#doors` nav's own opacity (always correctly `1`), never the individual `<a>` children where the frozen state actually lived. Fix (commit `23a3690a`): strip `threshold-active`/`room-exit` from the door links immediately on script load and on every `pageshow`, unconditionally (not gated on `e.persisted`). User-confirmed fixed live. Full debugging trail, including the false starts and the automated-browser-tool limitations that caused several dead ends, recorded in [[feedback_verify_by_execution]] for next time.

- **2026-07-22 (archive return-state restoration)** — **Archive browsing continuity preserved on return.** When a visitor opens an artwork from the archive and then clicks "[ ← BACK TO THE ARCHIVE ]", they now return to their exact prior browsing position (scroll, filters, sort, pagination) instead of the top of the page. Single-file fix to `archive.html` (115 lines): save state on exit via pagehide listener → sessionStorage; restore only on return from artwork (referrer guard prevents stale restoration after deep-link filters). Scroll targets the opened work's card (via data-id match), fallback to saved Y if filtered out. UI controls synced (search input, sort dropdown, chip active states). Tested live jfsn.com with collage filter + scroll → art0379 → back link: filter restored, scroll restored, first card in view. Commit `b878055d`, deployed 2026-07-22 14:03 EDT.

- **2026-07-22** — **Two relationship-focused interventions, both piloted narrowly and shipped after explicit approval: the Flooded Wing → Six Feet Away threshold, and a single-work creator-voice pilot on art0518.** Both grew out of a first-time-visitor journey review (desktop + mobile) that found the site's thematic connections were already strong (a work's page already links to its place in the fifty-year river; the Guernica Passage already links to the lost Guernica-scale wall in the Flooded Wing) but flagged two gaps: (1) Jeff's richest loss testimony (`stories.html#six-feet-away`) was reachable from the main visitor path only via a buried text link, and (2) individual artwork pages show machine description only — no creator voice exists anywhere on the public site, though the oral history holds it for specific works.
  **Threshold (commit `04d35a0d`):** added one link-block to `flooded-wing.html`, placed immediately after the curbside "discarded work" photograph and before the exit — "IN HIS OWN WORDS / [his verbatim first line] / SIX FEET AWAY →" into `stories.html#six-feet-away`. Reuses the room's own quote styling (Playfair italic in `--ink`, not recolored like a link) so it reads as testimony, not navigation. `stories.html` already linked back to the room, so the connection is now bidirectional. Verified live, desktop + mobile.
  **Creator-voice pilot (commit `7f7cc31c`), art0518 "Buy Me (Targets and Planes)" only:** before touching anything, re-verified the exact oral-history source (master-notes.md §29) — attribution to this specific work is secure and unhedged (unlike art0585/art0262, deliberately not touched). Added the verbatim testimony (*"desperate=)"* / *"not selling anything / trying to"*) to both surfaces a visitor can land on: the dynamic `artwork.html` (gated by `w.i === 'art0518'`, no other work affected) and the durable static `artworks/pages/art0518.html` (via a small `CREATOR_VOICE` per-id lookup added to `tools/generators/gen-artwork-pages.py` — not a catalog-schema field, since testimony is primary source, not descriptive metadata). Discovered along the way that the durable SEO pages don't compile the design system's text-size/colour utilities (true already on live jfsn.com, e.g. `art0003.html`, pre-existing and out of scope) — used self-contained inline styles so the block renders correctly and stays readable with JavaScript disabled, without expanding into fixing that pipeline. Verified: only art0518 emits the block (neighboring pages checked clean on both dev and prod), JSON-LD/canonical/nav untouched, `npm run verify` 0 failures. This is a pilot, not a rollout — no system was built, and generalizing to other works (audio-recording testimony, or the hedged art0585/art0262 cases) is an explicit future decision, not implied by this change.
  Session-end: git clean and pushed, both backup LaunchAgents healthy, cold backup to JEFFS-4TB verified (18976=18976 file count match), oral-history master-notes.md updated (§30) to record the threshold as an implementation note (no new testimony captured this session).

- **2026-07-21 (evening)** — **Design phase reopened; creative-experience review acted on (a11y, artwork-page, mobile).** Jeff explicitly reopened the design phase (closed 2026-07-08 by `STEWARDSHIP-DECLARATION.md`) — recorded in the new `docs/governance/DESIGN-REOPENED.md`, which supersedes the closure; the original declaration is kept intact as historical record with a superseded-notice. Prompted by a creative/UX review of the live site (homepage, archive, artwork page, The Current, About), which found the homepage's hover-preview and The Current's tick-to-image bloom were already fully built (review had missed testing hover states) and identified the single-artwork page as the strongest remaining opportunity. Shipped from that review: (1) accessibility — 6 animations on `archive.html` (`.vault-highlight`, `#results-header`, `.filter-pill`, `#grid.replacing`, `#count-label.tick`, `#featured-new`'s 4 staggered fades) were missing `prefers-reduced-motion` guards that every other page already had consistently; fixed. (2) `artwork.html` — fixed a decade-axis label collision (1970s/1980s overlapping, now computed from real pixel width instead of a fixed %), fixed the "you are here" marker overlapping the section heading above it, and added a warm wall-glow + cast shadow behind the work (tuned through several rounds against Jeff's live feedback) so it reads as hung on a wall rather than floating on flat black. (3) Mobile — a 320–375px header collision (site name touching/overlapping the exit link with zero gap) existed on 8 pages even though `artwork.html` already had the fix; propagated it. The desktop-tuned hero elliptical fade (`inset:-150%`) was swallowing most of the artwork above/below the hero text on narrow viewports on 6 room-hero pages; halved to `-75%` on mobile only. Also fixed `tools/utils/server.py`'s `ROOT` path (silently broken since the `tools/` reorg, `c28a4b33`, left it resolving to `tools/utils/` instead of the repo root) and updated `.claude/launch.json` to match. Commit `73b2eab7`, deployed and smoke-tested clean (all 10 checks); spot-verified live on jfsn.com directly (not just curl), including in a tab that had visited the site earlier the same session, confirming the service worker's network-first HTML strategy served the update with no `CACHE_V` bump needed.

- **2026-07-21 (later)** — **Generated artwork pages: reverse-image support + broken-image fix.** Staged audit of the public artwork-permalink surface found `artwork.html?id=` ships a JS-dependent "not in the record" fallback in its raw HTML (no-JS/crawler risk, invisible to normal JS-enabled visitors) and that the 1,086 generated `artworks/pages/artNNNN.html` static pages — more durable but unlinked from the live site — were missing reverse-image support for double-sided works and had a broken main-image path for art1085/art1086 specifically (`file` field lacked `.avif`, unlike every other record). Fixed `gen-artwork-pages.py` to derive all image paths from `art_id` (verified zero mismatches across all 1,086 records) and added a native `<details>`/`<summary>` reverse-side disclosure for `has_back` works — no JavaScript required, back image `loading="lazy"` so it's never fetched unless opened. Only art1085.html/art1086.html actually changed among the 1,086 regenerated pages. Verified live: front image loads, back image confirmed not fetched until disclosure opened, single-sided works emit no trace of the feature. Canonical-URL migration (making the generated pages authoritative) was investigated but explicitly deferred — see the open item above. Commit `9b14a4ab`, deployed and smoke-tested clean. Two small follow-on commits (`0a58578b`, `2d421784`, repo-only, not deployed) fixed stale `scripts/`-prefix paths in `DEPLOY.md`, `DISASTER-RECOVERY-CHECKLIST.md`, `CONSERVATION-CHECKLIST.md`, `SESSION_PROMPT.md`, and `CURRENT_STATE.md`, found by direct verification while deploying the feature commit.

- **2026-07-21** — **Archival-Integrity Remediation: Catalog restored to 1,086 works.** Fixed catalog count discrepancy (inconsistent references: 1,084 vs. 1,086 vs. 1,088). Removed pixel-identical duplicates art1087/art1088 (re-ingestion artifacts of art1085/art1086). Restored completeness of art1085/art1086 by syncing dims.json across config and root. Rebuilt catalog, current, API indexes to exactly 1,086 records. About page editorial: replaced vague "Exhibition Record" with creator-verified six-exhibition list (1978–2012), removed unsupported "Permanent Collections" section, removed three unverified "In His Own Words" quotations, added inquiry contact line. Fixed artwork.html orientation display to handle both full-word (vertical/horizontal/square) and short-code (v/h/s) values. Fixed verify.py schema_version crash on mixed int/str types. Updated all 1,084 static page references from 1,084 → 1,086. Regenerated all 1,086 artwork pages with dynamic footer count. Updated sitemap, privacy, about, archive, current, index meta descriptions/counts. Removed "OPEN DATA" wording from archive hero. Commits `ae6803fa` (1,134 files), `7c7e00e4` (education curriculum: added industrial design and graphic design to CIA focus). Deployed and live. 18 pre-existing metadata-quality errors on art1085/art1086 preserved for separate stewardship pass.

- **2026-07-20 (continuation session)** — **H1 Hero Animation: Orange sweep constrained to letter shapes only.** Orange gradient sweeps left-to-right on page load using `background-clip: text` to confine it to glyph shapes; never appears outside letters or on background. Took 8 iterations (clip-path polygons, scaleX transforms, text-shadow, drop-shadow filters, feGaussianBlur, pseudo-element overlays with blend modes) before landing on background-clip:text as the solution. Black text preserved on "Museum" highlight box via `-webkit-text-fill-color` override. Commit `8feb564d`; deployed and live. Key learning: subtle CSS animations are nearly impossible to verify in static screenshots — user needed to watch in real-time on the live site, which proved the animation was working all along (previous 7 approaches were technically correct but visually imperceptible in frozen frames).

- **2026-07-19 (same day, multi-round)** — **WOW Choreography Phase 1 Part 1 shipped: Temporal Passages live on current.html.** Fixed critical foundational anime.js v4 API misuse affecting all 14 choreography patterns (Timeline.add() syntax, property names, `.finished` thenable pattern). Fixed guernica-passage production bug (inline script race condition throwing ReferenceError, breaking veil + click-handler). Fixed current.html LCP regression (deferred anime-utils.umd.js load). Built + tested Series Highlight (for archive.html filter UI) + Work Arrival (for 1,084 artwork pages) patterns; wiring deferred per user request. All three WOW sequences verified frame-by-frame in real browser (Room Arrival, Archive Discovery, The Current). Lighthouse: 95–100 performance across all 6 pages, no regressions. Commits `fe3d8877`, `3d50de5b`, `d25c8ac1`. User explicitly wrapped here: "solid foundation shipped and ready for Jeff's visual verification + next-phase iteration."

- **2026-07-17/18** — **Both archive.html transition gaps (flagged 2026-07-17) fixed, deployed, and verified live.** Grid re-render (`24d4789e`): Load More now appends only the new slice instead of regenerating the whole grid, with a fade+rise stagger on new cards; genuine filter/search/sort/clear changes now cross-fade the grid instead of snapping. Mobile filters drawer (`364a1217`): converted from a hard `display:none`/`flex` toggle to a `grid-template-rows: 0fr → 1fr` reveal (~220ms ease-out), with a `prefers-reduced-motion` fallback to the old instant toggle. Both re-verified live on jfsn.com twice, including a full open/close/re-open pass on a fresh page load confirming `aria-expanded`, `grid-template-rows`, and `margin-top` all reset correctly with no residual gap — not just checked once.
- **2026-07-17 (latest)** — **Sitewide interaction-design review (archive.html + the five room pages) + one fix shipped.** Reviewed `archive.html`'s card grid/filter/search interactions (found real gaps — abrupt full-grid replace on every filter/search/Load More with no transition or stagger, and a hard `display:none`/`flex` snap on the mobile filters drawer with zero motion — both fixed the next day, see entry above) and the five room pages' own body-content link/card treatment (found already mature: `working-history.html`'s `.rec` cards already used the same arrow-shift pattern built earlier this session; `hall-of-openings.html`'s `.op` cards already match `archive.html`'s card choreography; `flooded-wing.html`'s wall-drag installation already has proper `cursor:grab`/`grabbing` states — confirmed these were appropriately built, not gaps). One real, verified gap found and fixed: `current.html`'s `#card` (the focal work card shown while scrolling THE CURRENT's fifty-year river) gave zero hover/focus feedback on the image or title — only its small "[ OPEN ]" caption line responded — unlike `archive.html`'s fuller card elevation. Extended the same proven pattern (accent border, 2px lift, warm box-shadow) to `#card .mat` (`a26cc006`). Verification took longer than expected: real `:hover` couldn't be reliably simulated on this element since its position is reassigned every animation frame by the page's own scroll-river loop (confirmed via `getAnimations()`/timing, not a real defect); also hit and correctly diagnosed a stale local-server-cache false negative from repeated testing against the same origin earlier in the session. Confirmed via `:focus-visible` (same CSS rule) read in separate script calls, avoiding same-tick timing artifacts. Deployed and smoke-tested clean.
- **2026-07-17 (later)** — **Homepage door-nav interaction design pass + a real production bug found and fixed.** Design-reviewed the homepage's five-room door nav (Awwwards/interaction-design lens) and implemented the resulting recommendations across several rounds: removed a decorative wavy connector SVG; shifted hero title/scroll-cue 50px; added roman-numeral sequence markers (I–V, `aria-hidden`); gave each room a felt-before-hover "rest state" (border-left color-alpha via `color-mix()` — sub-pixel border *widths* don't work, browsers snap them to whole device pixels, learned by testing); fixed a previously-dead hover-preview feature (`--preview-url` was being set by JS but no CSS ever consumed it); added arrow micro-shift + sub-label settle on hover; added pointer-proximity anticipation (a row senses the pointer before actual hover, `hover:hover`+`pointer:fine` gated); added an asymmetric-return animation (the room a visitor just left draws in first, via `document.referrer`). **Real bug found while verifying keyboard parity:** `index.html`'s door click handler referenced an undeclared variable `v` (name collision with an unrelated `v` scoped to a different `<script>` block), throwing `ReferenceError` on every door click — mouse or keyboard — which silently killed the entire "Threshold Passage" animation (border sweep, sibling cascade, room-colored veil) in production the whole time; masked because a separate sitewide page-transition handler took over navigation instead, so nothing looked broken. Fixed, and also fixed a second, deeper issue the crash was hiding: that sitewide handler raced the door animation and always won (200ms vs. the door animation's 400ms) — moved the door handler to a document-level capture-phase listener with `stopImmediatePropagation()` (matching this codebase's own documented pattern for beating that handler) so the full animation now completes before navigating. Verified via `window.onerror` capture and timed `sessionStorage` probes, not just visual screenshots. A follow-up review of `artwork.html` (all 1,084 pages) and the five room pages found and fixed three more real gaps: Previous/Next links had zero hover feedback despite being the single most-used control in the archive; keyboard arrow-key walking bypassed the transition veil entirely even though the page's own on-screen hint invites keyboard use; and Guernica Passage's arrival had almost no entrance choreography compared to its three sibling rooms (matches an edge case already flagged in a prior session, now fixed to match the shared pattern). Also rewrote `docs/current/SESSION_END_PROCEDURES.md` (the prior version was Session-63-era and referenced files that no longer exist) and fixed a real bug in `scripts/session-end.sh` that had been silently skipping the cold backup step every time due to a bad relative path. Commits `52ba32b9` through `184ce16b`; deployed and smoke-tested clean after every change.
- **2026-07-17** — **Removed unused `artist-config.js` + fixed 3 stale `deploy-hostgator.sh` smoke-test checks.** JS audit found `artist-config.js` (generated by `build_catalog.py` "for browser-side consumption") was never actually loaded by any page — no `<script>` tag, no other JS reading `window.ARCHIVE_CONFIG`. Deleted the file, its generation block in `build_catalog.py`, and the dangling `"main"` field in `package.json` (`52ba32b9`). That deploy surfaced 3 pre-existing false-positive smoke checks, unrelated to the removal: `core.bundle.js` (file hasn't existed since the legacy design system was retired — swapped for `artwork-page-min.js`, which now carries `showToast`), the 404 page (title is "Not Found", not literal "404"), and `artwork.html` (the unbuilt template never included `site.min.css` — swapped for `room-veil`, content it actually has). Fixed across two commits (`cec70763`, `8afb95f8`), redeployed, all 10 smoke checks now pass clean.
- **2026-07-16 (later)** — **Homepage LCP fixed: 5.1s → 1.9s, Lighthouse 81 → 100.** Root cause: hero image fetched 3× eagerly (LCP element + 2 decorative wing flankers, same 314KB file). Wired in pre-built-but-unused LCP assets (`art1010-hero-lcp.avif`/`-m.avif`) via srcset; wings switched to lazy-load. Also: hero highlight-box color unified to orange across all 5 room pages, archive.html search bar surfaced above the fold, both backup LaunchAgents found unloaded from launchd since 2026-07-08 and reloaded + verified with live kickstarted runs (see CURRENT_STATE.md for full detail).
- **2026-07-14 to 2026-07-16** — **Permanent Museum Approved** (curatorial review complete, 2026-07-15; all five main rooms certified without revision required). Hero implementation complete across four rooms (The Studio, Guernica Passage, Hall of Openings, Flooded Wing). Border Grammar analysis + implementation for doors as sole interaction language. Documentation cleanup: renamed duplicate verification-standard file, archived 8 old session closeout docs, deleted orphan assets/files. Commit `d4ad53b6`.
- **2026-06-22/23** — Full documentation/preservation audit (every `.md` file reviewed) followed by **Netlify and the Companion AI chat feature removed entirely** — Netlify had no git integration and was a recurring source of deploy friction; Companion only ran as a Netlify Function, so dropping one meant dropping the other. HostGator is now the only host. Touched: `companion.html` + `netlify/` deleted, `tools/generators/gen-artwork-pages.py`'s static-page template fixed (all 1,084 pages regenerated), `session-end.sh`/`stamp-nav.sh`/`build_catalog.py`/`verify_deploy.py`/`make_handoff.py` cleaned up, 10 active docs corrected (including rewriting `DISASTER-RECOVERY-CHECKLIST.md`'s Scenario B, which depended on the now-gone Netlify mirror as the "immediately reachable" fallback). Also caught and fixed a 6-day-old propagation gap from the 2026-06-16 domain-ownership correction (`docs/KNOWLEDGE-AT-RISK-INVENTORY.md` still listed "the domain-friend's name" as an open to-do). Committed `047aab54` + `139a2ce9`, pushed, deployed to jfsn.com (verified live), 4TB backup verified (file counts matched after a drive reseat).
- **2026-06-21** (sessions 76–78) — Homepage Selected Works simplification: removed Session-77 interaction layer (ripple/badge/swatch/peek-modal); kept image + always-visible caption + link. CLAUDE.md updated to retire the "default to removal" framing as an over-correction (motion designer's craft restored as the design stance).
- **2026-06-20** (session 75) — Selected Works masonry redesign to match archive.html (CSS Columns 4→3→2, simplified card structure). series-index.html responsive padding + image overlay removal.
- **2026-06-19** (session 74) — Archive grid masonry fix (CSS Columns), saturation overlay removed sitewide (true vibrant color revealed), series page tooltip cleanup.

Older entries removed per this file's own rule ("Delete completed items; history lives in git log"). If a longer narrative log is wanted, see `docs/sessions-archive.md`.

---

## How to use this list

Start each session: paste the start prompt → `Read CURRENT_STATE.md and IMPROVEMENTS.md. Summarize open items by priority, flag anything stale, then ask what I want to work on.`

Add new ideas here any time. Delete completed items; history lives in git log.
