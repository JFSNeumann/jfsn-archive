# ── Images (too large for git — already on server) ───────────────────────────
artworks/full/*.avif
artworks/thumbs/*.avif
artworks/mini/*.avif

# ── FTP credentials (never commit) ───────────────────────────────────────────
.ftp.env

# ── Local-only / generated ───────────────────────────────────────────────────
__pycache__/
artworks/__pycache__/
artworks/logs/
*.pyc
.DS_Store

# ── Node / Tailwind build ─────────────────────────────────────────────────────
node_modules/
package-lock.json

# ── Private tool folders ──────────────────────────────────────────────────────
.claude/
.netlify/

# ── Old site (197MB — deployed via FTP, not via git) ─────────────────────────
old-site/
*.bak
*.bak2
*.bak3
*.bak4
*.backup.json
