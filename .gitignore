# ── Images (too large for git — already on server) ───────────────────────────
artworks/full/*.avif
artworks/thumbs/*.avif
artworks/mini/*.avif

# ── FTP credentials (never commit) ───────────────────────────────────────────
.ftp.env

# ── Local-only / generated ───────────────────────────────────────────────────
__pycache__/
artworks/__pycache__/
artworks/logs/
artworks/.asset_hash
*.pyc
.DS_Store
*.report.html

# ── Node / Tailwind build ─────────────────────────────────────────────────────
node_modules/
package-lock.json

# ── Private tool folders ──────────────────────────────────────────────────────
.claude/
.netlify/

# ── Old site (197MB — local-only as of 2026-06-16, no longer deployed) ───────
old-site/
*.bak
*.bak2
*.bak3
*.bak4
*.backup.json

# ── Credential-bearing generated artifacts (never commit) ────────────────────
JFSN-Archive-Handoff-Allison.pdf

# ── Design concepts / Stitch exports (local-only; repo is public) ────────────
design-concepts/
